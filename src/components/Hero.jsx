function Hero() {
  return (
    <section className="hero" aria-labelledby="hero-title">
      <div>
        <p className="eyebrow">NUS Orbital 2026</p>
        <h1 id="hero-title">Diffriendtiate</h1>
        <p className="hero-copy">
          A barebone React starter for the team project.
        </p>
      </div>

      <a className="primary-action" href="#next-steps">
        View starter
      </a>
    </section>
  );
}

export default Hero;
